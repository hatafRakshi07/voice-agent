fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Database (SQLite — zero-config, no server needed)
aiosqlite>=0.20.0

# ── Local Speech-to-Text (Whisper) ──────────────────────────────────────────
# faster-whisper: CTranslate2-based Whisper, runs on CPU (int8) or GPU
faster-whisper>=1.0.3

# WebRTC Voice Activity Detection (default VAD)
webrtcvad-wheels>=2.0.14

# Silero VAD (optional neural VAD — enable with USE_SILERO_VAD=true)
silero-vad>=5.1

# ── LLM Providers ────────────────────────────────────────────────────────────
# Ollama REST client
httpx==0.28.1
aiohttp==3.11.9

# Google Gemini API (LLM_PROVIDER=gemini)
google-generativeai>=0.8.0

# ── Local TTS (Coqui XTTS-v2) ────────────────────────────────────────────────
# TTS installs torch automatically; pin to CPU wheels in Dockerfile
# transformers pinned: BeamSearchScorer removed in 4.40+, Coqui TTS requires <4.40
TTS>=0.22.0
transformers==4.39.3

# ── Audio processing ─────────────────────────────────────────────────────────
numpy>=1.22.0
soundfile>=0.12.1
scipy>=1.11.0
librosa>=0.10.0

# ── Async / WebSocket ─────────────────────────────────────────────────────────
websockets>=13.1
aiofiles>=24.1.0

# ── Whisper fine-tuning ───────────────────────────────────────────────────────
datasets>=2.16.0
evaluate>=0.4.1
jiwer>=3.0.3
accelerate>=0.27.0
ctranslate2>=4.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
loguru==0.7.2
python-multipart>=0.0.18
